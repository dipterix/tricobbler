# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_context_attachment_list
description: List all attachments (recorded results) in the current workflow context.
  Returns metadata for each attachment including stage, state, agent ID, attempt number,
  and the attachment identifier needed for retrieval. This tool works at any accessibility
  level. When accessibility is not "all", a note is included indicating the agent
  cannot read attachment content.
parameters:
  type: object
  properties:
    reindex:
      type: boolean
      description: Whether to reload the index from disk (default FALSE). Set to TRUE
        if attachments may have been modified externally.
      default: no
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    note:
      type: string
      description: Character, present when accessibility is not "all", indicating
        the agent cannot read attachment content
    attachments:
      type: object
      description: List of attachment metadata, each with stage, state, agent_id,
        current_attempt, filename
    count:
      type: integer
      description: Integer, number of attachments found
    context_id:
      type: string
      description: Character, the context identifier
    success:
      type: boolean
      description: Logical, whether the operation completed successfully
output_format: json
