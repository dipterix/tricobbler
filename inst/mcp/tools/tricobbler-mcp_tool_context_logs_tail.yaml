# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_context_logs_tail
description: Read and parse log file contents from the end of the log file. Returns
  structured log entries with level, time, caller, and content. This tool is useful
  for reviewing recent workflow activity or debugging the latest issues.
parameters:
  type: object
  properties:
    levels:
      type: array
      description: 'Log levels to include. Defaults to all levels: TRACE, DEBUG, INFO,
        WARN, ERROR, FATAL.'
      items:
        type: string
      default: TRACE
    pattern:
      type: string
      description: Optional regex pattern to filter log content. Only entries matching
        the pattern in their content will be returned.
    skip_lines:
      type: integer
      description: Number of lines to skip from the end (default 0). Use this for
        pagination when reading older entries.
      default: 0.0
    max_lines:
      type: integer
      description: Maximum number of lines to read (default 300).
      default: 300.0
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    entries:
      type: object
      description: List of log entries, each with line_no, level, time, caller, content
    count:
      type: integer
      description: Integer, number of log entries returned
    context_id:
      type: string
      description: Character, the context identifier
    success:
      type: boolean
      description: Logical, whether the operation completed successfully
output_format: json
