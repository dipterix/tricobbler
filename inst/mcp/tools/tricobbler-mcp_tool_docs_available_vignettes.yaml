# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_docs_available_vignettes
description: Get a list of all available vignettes (long-form documentation, tutorials,
  and guides) for a specific R package. Vignettes provide detailed examples and workflows
  beyond standard help pages. To read a vignette, use tricobbler-mcp_tool_docs_vignette(package_name,
  vignette).
parameters:
  type: object
  properties:
    package_name:
      type: string
      description: The exact name of the package to list vignettes from (e.g., "tricobbler",
        "dplyr", "ggplot2"). This parameter is required.
      examples:
      - tricobbler
      - dplyr
      - ggplot2
  required:
  - package_name
category: docs
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    count:
      type: integer
      description: Integer, total number of vignettes found
    vignettes:
      type: object
      description: Data frame with columns name (vignette filename without extension),
        title (vignette title), file (full filename), and pdf (PDF path if available)
    package:
      type: string
      description: Character, the package name that was queried
    success:
      type: object
      description: Logical, whether vignette list was retrieved successfully
implementation_example: |
  # Example output from calling the tool
  tricobbler:::mcp_tool_docs_available_vignettes("data.table")
  #
  #> $success
  #> [1] TRUE
  #> 
  #> $package
  #> [1] "data.table"
  #> 
  #> $vignettes
  #>                                             name
  #> 1                         datatable-benchmarking
  #> 2                              datatable-reshape
  #> 3                     datatable-fread-and-fwrite
  #> 4                                  datatable-faq
  #> 5                            datatable-importing
  #> 6                                datatable-intro
  #> 7                                datatable-joins
  #> 8                     datatable-keys-fast-subset
  #> 9                          datatable-programming
  #> 10                 datatable-reference-semantics
  #> 11 datatable-secondary-indices-and-auto-indexing
  #> 12                            datatable-sd-usage
  #>                                                         title    package
  #> 1                      Benchmarking data.table (source, html) data.table
  #> 2        Efficient reshaping using data.tables (source, html) data.table
  #> 3                     Fast Read and Fast Write (source, html) data.table
  #> 4  Frequently Asked Questions about data.table (source, html) data.table
  #> 5                         Importing data.table (source, html) data.table
  #> 6                   Introduction to data.table (source, html) data.table
  #> 7                          Joins in data.table (source, html) data.table
  #> 8     Keys and fast binary search based subset (source, html) data.table
  #> 9                    Programming on data.table (source, html) data.table
  #> 10                         Reference semantics (source, html) data.table
  #> 11         Secondary indices and auto indexing (source, html) data.table
  #> 12                 Using .SD for Data Analysis (source, html) data.table
  #> 
  #> $count
  #> [1] 12
  #>
execution_time: 0.003
