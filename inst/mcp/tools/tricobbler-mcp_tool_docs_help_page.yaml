# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_docs_help_page
description: Retrieve and format help documentation for an R function or topic from
  a specific package or search across all packages. Returns the help content in human-readable
  text format suitable for AI assistance.
parameters:
  type: object
  properties:
    package_name:
      type: string
      description: 'The exact name of the package to search in (e.g., "tricobbler").
        If empty string "" (default), searches across all loaded packages. Specifying
        the package name is recommended for faster and more accurate results. Examples:
        "tricobbler", "base", "utils".'
      default: ''
      examples:
      - tricobbler
      - tricobbler
      - base
      - utils
    topic:
      type: string
      description: 'The name of the help topic or function to look up. This can be
        a function name, class name, or any documented topic. Examples: "pipeline",
        "PipelineTools", "mcp_describe".'
  required:
  - topic
category: docs
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    content:
      type: string
      description: Character, formatted help documentation as plain text
    package:
      type: string
      description: Character, the package where the topic was found (if specified
        or discovered)
    topic:
      type: string
      description: Character, the topic that was searched for
    success:
      type: boolean
      description: Logical, whether the help page was found and retrieved
implementation_example: |
  # Example output from calling the tool
  res <- tricobbler:::mcp_tool_docs_help_page("mean", "base")
  strsplit(gsub("[^a-zA-Z0-9 .\n\t-]+", "", res$content), "\n")[[1]]
  #
  #> [1] "Arithmetic Mean"                                       
  #> [2] ""                                                      
  #> [3] "Description"                                           
  #> [4] ""                                                      
  #> [5] "     Generic function for the trimmed arithmetic mean."
  #>  [ reached 'max' / getOption("max.print") -- omitted 51 entries ]
execution_time: 0.446
