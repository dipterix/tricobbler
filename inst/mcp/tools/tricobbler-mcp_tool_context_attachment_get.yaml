# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_context_attachment_get
description: Retrieve a specific attachment (recorded result) from the current workflow
  context. Returns the full attachment data including the result object, description,
  and metadata. Requires policy accessibility level "all". Returns an error if accessibility
  is "logs" or "none".
parameters:
  type: object
  properties:
    attachment_id:
      type: string
      description: 'The attachment identifier. This is the filename value from mcp_tool_context_attachment_list
        or the identifier logged when a result is recorded. Format: \verb{[stage][state][agent_id]_YYMMDDTHHMMSS_attempt}.'
  required:
  - attachment_id
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    attachment:
      type: object
      description: List, the attachment data including result, description, stage,
        state, agent_id, current_attempt, and ._timestamp
    attachment_id:
      type: string
      description: Character, the queried attachment identifier
    context_id:
      type: string
      description: Character, the context identifier
    success:
      type: boolean
      description: Logical, whether the operation completed successfully
output_format: json
