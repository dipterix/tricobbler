# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_read_package_file
description: Read a specific file from an R package directory and return its content
  as text. Supports reading partial content with line-based pagination for large files.
  Useful for inspecting package vignettes, documentation source files, datasets, and
  other resources.
parameters:
  type: object
  properties:
    'n':
      type: integer
      description: 'The maximum number of lines to read. Default is 20. Combined with
        begin, this enables reading files in chunks. Example values: 50, 100, 500.'
      default: 20.0
      examples:
      - '50'
      - '100'
      - '500'
    begin:
      type: integer
      description: 'The starting line number to read from (1-indexed). Default is
        1L (including the first line. Use this for pagination or reading specific
        sections. Example values: 1, 50, 100.'
      default: 1.0
      examples:
      - '1'
      - '50'
      - '100'
    package_name:
      type: string
      description: The exact name of the package containing the file (e.g., "tricobbler",
        "dplyr", "ggplot2"). This parameter is required.
      examples:
      - tricobbler
      - dplyr
      - ggplot2
    relpath:
      type: string
      description: 'The relative path to the file within the package directory. Must
        not contain ".." or special characters (only letters, digits, underscores,
        dashes, dots, and path separators allowed). Use tricobbler-mcp_tool_search_package_files
        to discover available files. Examples: "inst/examples/demo.R", "doc/introduction.Rmd".'
  required:
  - relpath
  - package_name
category: discovery
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    content:
      type: array
      description: Character vector, file content with each element representing one
        line
      items:
        type: string
    range:
      type: array
      description: Integer vector of length 2, one-indexed range [start, end) indicating
        the actual lines read
      items:
        type: integer
    relpath:
      type: string
      description: Character, the relative path that was read
    package:
      type: string
      description: Character, the package name
    success:
      type: boolean
      description: Logical, whether the file was read successfully
implementation_example: |
  # Example output from calling the tool
  tricobbler:::mcp_tool_read_package_file(
    relpath = "mcp/DEVELOPER_GUIDE.md",
    package_name = "tricobbler",
    begin = 1,
    n = 3
  )
  tricobbler:::mcp_tool_read_package_file(
    relpath = "mcp/SPECIFICATION-TOOLS.md",
    package_name = "tricobbler",
    begin = 1,
    n = 3
  )
  #
  #> $success
  #> [1] TRUE
  #> 
  #> $package
  #> [1] "tricobbler"
  #> 
  #> $relpath
  #> [1] "mcp/SPECIFICATION-TOOLS.md"
  #> 
  #> $range
  #> [1] 1 3
  #> 
  #> $content
  #> [1] "# RAVE MCP Tools YAML Specification"                                                                                                     
  #> [2] ""                                                                                                                                        
  #> [3] "This document describes the YAML format specification for RAVE MCP (Model Context Protocol) tools generated from roxygen2 documentation."
  #>
execution_time: 0.035
