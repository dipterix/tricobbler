# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_context_logs_search
description: Search log file contents using a regex pattern. Reads the entire log
  (up to max_lines) and returns entries matching the pattern. This tool is useful
  for finding specific events, errors, or agent activities across the entire log history.
parameters:
  type: object
  properties:
    levels:
      type: array
      description: 'Log levels to include. Defaults to all levels: TRACE, DEBUG, INFO,
        WARN, ERROR, FATAL.'
      items:
        type: string
      default: TRACE
    max_lines:
      type: integer
      description: Maximum number of lines to search through (default 1000). Reads
        from the end of the log.
      default: 1000.0
    pattern:
      type: string
      description: Regex pattern to search for in log content. This parameter is required.
  required:
  - pattern
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    entries:
      type: object
      description: List of matching log entries, each with line_no, level, time, caller,
        content
    count:
      type: integer
      description: Integer, number of matching log entries
    pattern:
      type: string
      description: Character, the search pattern used
    context_id:
      type: string
      description: Character, the context identifier
    success:
      type: boolean
      description: Logical, whether the operation completed successfully
output_format: json
