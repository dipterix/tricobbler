# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_config_set_outputs
description: '(This function is intended for \verb{MCP} calls directly, not for end-users).
  Adjusts runtime settings for \verb{MCP} tool output formatting. Controls when results
  are returned as JSON versus formatted text. When JSON serialization fails (due to
  size or complexity), tools automatically fall back to human-readable text output
  via mcp_describe. ## When to Use Need to adjust output format for large results
  or token optimization. Typically not needed unless outputs fail serialization or
  are too verbose.'
parameters:
  type: object
  properties:
    tricobbler.mcp_describe.max_size:
      type: integer
      description: '. Maximum number of lines for text output when using mcp_describe.
        Default is NULL (no change). Controls verbosity of fallback text formatting.
        Typical range: 50-200.'
category: configuration
returns:
  type: object
  properties:
    settings:
      type: object
      description: List, all current output settings with their values
    message:
      type: string
      description: Character, confirmation message
    success:
      type: boolean
      description: Logical, always TRUE
implementation_example: |
  # This function is intended for MCP calls directly, not for end-users
  tricobbler:::mcp_tool_config_set_outputs()
  #
  #> $success
  #> [1] TRUE
  #> 
  #> $message
  #> [1] "No settings changed (returning current configuration)"
  #> 
  #> $settings
  #> $settings$tricobbler.mcp_describe.max_size
  #> [1] 100
  #> 
  #>
execution_time: 0.001
