# This file is automatically generated by tricobbler::mcptool_build(). 
# Please never edit this file by hand!
name: tricobbler-mcp_tool_search_package_info
description: Retrieve detailed metadata and information about a specific R package,
  including version, dependencies, maintainer, and description. This provides comprehensive
  package details beyond basic help topics.
parameters:
  type: object
  properties:
    package_name:
      type: string
      description: The exact name of the package to get information about (e.g., "tricobbler",
        "ggplot2", "dplyr"). This parameter is required.
      examples:
      - tricobbler
      - ggplot2
      - dplyr
  required:
  - package_name
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    info:
      type: object
      description: 'List containing package metadata: version, title, description,
        author, maintainer, license, depends, imports, suggests, url, bugreports,
        and other DESCRIPTION fields'
    package:
      type: string
      description: Character, the package name
    success:
      type: boolean
      description: Logical, whether package information was retrieved successfully
implementation_example: |
  # Example output from calling the tool
  res <- tricobbler:::mcp_tool_search_package_info("tricobbler")
  strsplit(res$info$description, "\n")[[1]]
  #
  #> [1] "Provides a two-tier architecture for building multi-agent AI"               
  #> [2] "workflows combining immutable policy definitions (S7 classes) with mutable" 
  #> [3] "execution state (R6 classes). Defines workflow blueprints through master"   
  #> [4] "policies (stages vocabulary) and state policies (stage implementations with"
  #> [5] "priority-based ordering), validates completeness via manifests, and"        
  #>  [ reached 'max' / getOption("max.print") -- omitted 4 entries ]
execution_time: 0.001
