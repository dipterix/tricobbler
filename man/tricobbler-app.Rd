% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-app.R
\name{tricobbler-app}
\alias{tricobbler-app}
\alias{tricobbler_app}
\title{Launch \pkg{tricobbler} Chat Application}
\usage{
tricobbler_app(
  client = NULL,
  tools = TRUE,
  skills = TRUE,
  workflows = TRUE,
  system_prompt = NULL,
  title = "TriCobbler",
  ...
)
}
\arguments{
\item{client}{an \code{ellmer::Chat} object or \code{NULL}.
If provided, the chat session is already configured. If
\code{NULL}, users select a provider and model from the
\verb{UI}.}

\item{tools, skills, workflows}{passed to
\code{\link{tricobbler_chat_server}()}; control resource
discovery scope. See \code{\link{discover_resources}()} for
details.}

\item{system_prompt}{character or \code{NULL}, default system
prompt for the chat.}

\item{title}{character, window and sidebar title
(default: \code{"TriCobbler"}).}

\item{...}{additional arguments passed to
\code{shiny::shinyApp()}.}
}
\value{
A \code{shiny::shinyApp} object (launched interactively
or returned for deployment).
}
\description{
Launches a standalone \pkg{shiny} application wrapping the
\pkg{tricobbler} chat module. This is a convenience function
similar to \code{shinychat::chat_app()} but with the full
\pkg{tricobbler} sidebar for resource discovery and workflow
orchestration.
}
\examples{
\dontrun{
# With existing client
chat <- ellmer::chat_openai(model = "gpt-4o")
tricobbler_app(chat)

# Without client (select provider in UI)
tricobbler_app()

# Restrict to specific resources
tricobbler_app(
  tools = "tricobbler",
  skills = "/path/to/skills",
  workflows = FALSE
)
}

}
