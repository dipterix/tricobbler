% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper-agents.R
\name{create_chat}
\alias{create_chat}
\title{Create a Chat Object from Provider Selection}
\usage{
create_chat(provider, model = NULL, system_prompt = NULL, base_url = NULL, ...)
}
\arguments{
\item{provider}{character, the provider suffix (e.g., \code{"openai"},
\code{"anthropic"}, \code{"ollama"}). Must match one of the
values returned by \code{\link{provider_choices}()}.}

\item{model}{character or \code{NULL}, the model identifier (e.g.,
\code{"gpt-4o"}, \code{"claude-sonnet-4-20250514"}). If \code{NULL},
the \pkg{ellmer} provider default is used.}

\item{system_prompt}{character or \code{NULL}, optional system prompt
to configure the chat.}

\item{base_url}{character or \code{NULL}, optional base URL override
(useful for \code{Ollama} or custom endpoints).}

\item{...}{additional arguments forwarded to the \pkg{ellmer}
\code{chat_*()} constructor.}
}
\value{
An \code{ellmer::Chat} object.
}
\description{
Constructs an \code{ellmer::Chat} object from a provider suffix and
model name. Used by the \code{Shiny} module when the user selects
a provider from the \verb{UI} selection menu.
}
\examples{
\dontrun{
# Requires API key environment variable
chat <- create_chat("openai", "gpt-4o")
chat <- create_chat("ollama", "llama3.1",
                    base_url = "http://127.0.0.1:11434")
}

}
